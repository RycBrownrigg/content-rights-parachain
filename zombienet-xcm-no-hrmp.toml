# Zombienet config for XCM testing — NO HRMP channels at genesis.
# Tests whether parachains can produce blocks without genesis HRMP channels.

[settings]
node_verifier = "None"

[relaychain]
default_command = "polkadot-sdk/target/release/polkadot"
default_args = [ "-lparachain=debug", "--rpc-external", "--workers-path", "polkadot-sdk/target/release" ]
chain = "rococo-local"

[[relaychain.nodes]]
name = "alice"

[[relaychain.nodes]]
name = "bob"

# Parachain A: Content Rights chain (para ID 100)
[[parachains]]
id = 100
chain = "local"
command = "target/release/parachain-template-node"
args = ["--rpc-external", "--unsafe-rpc-external", "--force-authoring"]
name = "content-rights-parachain"

[[parachains.collators]]
name = "collator-a"
command = "target/release/parachain-template-node"
rpc_port = 9990
args = ["--rpc-external", "--unsafe-rpc-external", "--force-authoring"]
